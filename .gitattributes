# 仓库里统一存 LF，检出时按平台还原。
# 没有这个文件，Windows 上 `git add` 会不停地警告 "LF will be replaced by CRLF"。
* text=auto eol=lf

# Windows 批处理必须是 CRLF：cmd.exe 对 LF 结尾的 .bat 行为不可靠
# （最典型的是 goto/label 找不到）。console.bat 是唯一的入口，坏了整个项目就没法用。
*.bat text eol=crlf

# 二进制/不该被行尾转换的
*.png binary
*.jpg binary
*.ico binary
