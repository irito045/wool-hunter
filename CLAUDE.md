# CLAUDE.md

This repository uses `AGENTS.md` as the single source of truth for agent
instructions.

Claude Code users should read [`AGENTS.md`](AGENTS.md). Do not duplicate the
full guidance here; keeping two long instruction files in sync has already
caused stale guidance.
